/*
var test = require('../lib/test');
var vows = require('vows');
vows.describe('generic intent'
).addBatch(
    test.assertStatement({
        flow: [
            'pause music'
        ],
        action: 'pause',
        context: 'music',
        debug: true
    })
).export(module);
*/
